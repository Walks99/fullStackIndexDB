{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is a simple backend server for passing data to a frontend application. The frontend application will then store the data in an IndexDB database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "beniBoi",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^3.1.0"
  }
}
